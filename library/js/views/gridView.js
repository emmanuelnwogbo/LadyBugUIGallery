//grid display by default, any photo in the grid
//will trigger the lightbox when clicked
const gridView = () => `
  <div class="grid--view">
    hello there grid view
  </div>
`

export default gridView;